{
  "name": "pax-cookbook-app-shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PAX Cookbook product shell (React-in-WebView2). Build-time only; the static bundle is emitted into app/web/app and served by the in-process broker. No runtime CDN or network dependency. Reproducible builds use 'npm ci' against the committed package-lock.json, then 'npm run build'.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
