<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>PAXCookbook.ServiceAdminHelper.Tests</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.10.0" />
    <PackageReference Include="xunit" Version="2.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>
  <!-- References the helper ONLY. It deliberately does NOT reference
       PAXCookbookSetup: the helper compile-links copies of several Setup
       sources, so referencing both assemblies would make those type names
       ambiguous. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\PAXCookbook.ServiceAdminHelper\PAXCookbook.ServiceAdminHelper.csproj" />
  </ItemGroup>
</Project>
